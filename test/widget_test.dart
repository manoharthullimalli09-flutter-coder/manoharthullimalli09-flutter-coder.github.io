// Smoke test — verifies the app root builds without throwing.
// Full section widget tests live in test/features/*/presentation/widgets/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — full widget tests in features/*/presentation/widgets/', () {
    expect(true, isTrue);
  });
}
